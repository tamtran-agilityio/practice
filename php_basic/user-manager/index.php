<?php
  echo "SSS";
  // $server = '127.0.0.1:8080'; //HOST NAME.
  // $db_name = 'root'; //Database Name
  // $db_password = 'root'; //Database Password
  // $database = 'example_db'; //Database Username
  // $mysqli = new mysqli($server, $db_username, $db_password, $database);
  // if ($mysqli->connect_errno()) {
  //   throw new Exception("MySQL connection error:" . $mysqli->connect_error);
  // } else {
  // }
  // try {
  //   $link = mysql_connect($host, $db_name, $db_password);
  //   if (!$link) {
  //     die('Could not connect: ' . mysql_error());
  //   }
  // } catch (Exception $e) {
  //   exit('Error Connecting To DataBase' .$e);
  // }
  // echo 'Connected successfully';
  // mysql_close($link);
  // try
  // {
  //   $pdo = new PDO('mysql:host='. $host .';dbname='.$db_name, $db_username, $db_password);
  // }
  // catch (PDOException $e)
  // {
  //   exit('Error Connecting To DataBase' .$e);
  // }
?>